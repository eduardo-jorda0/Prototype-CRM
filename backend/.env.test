PORT=5000
# SQLite em memória para testes rápidos (sem dependência de banco remoto)
# Nota: Em produção, sempre use um banco PostgreSQL remoto
DATABASE_URL="file:./test.db"
JWT_SECRET="super-secret-jwt-key-prototype-crm-2026"
UPLOAD_DIR="./uploads"
NODE_ENV="test"
